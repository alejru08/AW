<!DOCTYPE html>
<html lang="es">

    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="/css/Login.css" />
        <title>404</title>
    </head>

    <body>
        <div class="encabezado">
            <h1>404</h1>
        </div>
        <div class = "formulario">
            <form action="/login" class="formulario" method="POST">
                <div class="user">
                    <div class="cabecera"><label>Correo *</label></div>  
                    <div>
                        <input type="email" class="datos" id="email" name="email" 
                        pattern="^[a-zA-Z0-9_]*$+@[4][0][4][.][eE][sS]" required>
                    </div>                      
                </div>
                <div class="user">
                    <div class="cabecera"><label>Contraseña *</label></div>   
                    <div class="datos"><input type="password" class="datos" id="password" name="password"
                        pattern="^[a-zA-Z0-9_]*$" required></div> 
                </div>
                <% if(errorMsg) { %>
                    <label class="error"><%= errorMsg %></label> 
                <% } %>
                <input type="submit" class="boton inicio" value="Iniciar Sesión">
            </form>
            <a href="/signUp"><button class="boton registro">Registrarse</button></a>
        </div>
    </body>
    
</html>